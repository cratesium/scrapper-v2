name: Daily Job Scraper

on:
  schedule:
    # Runs at 09:00 UTC every day
    - cron: '0 9 * * *'
  workflow_dispatch: # Allows you to run the script manually from the Actions tab

jobs:
  scrape-and-email:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10' # Adjust to your local Python version if needed

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Job Scraper
      env:
        GMAIL_USER: ${{ secrets.SENDER_EMAIL }}
        GMAIL_APP_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        TO_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
      run: |
        python main.py
